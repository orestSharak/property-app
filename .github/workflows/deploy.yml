name: Manual Deployment

on:
  # Manual trigger with deployment options
  workflow_dispatch:
    inputs:
      target:
        description: 'Deployment Target'
        type: choice
        required: true
        default: 'hosting'
        options:
          - hosting
          - functions
          - all

jobs:
  deploy:
    name: Deploy to Firebase
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # --- Download and Extract Artifact ---
      - name: Download Build Artifact from CI
        uses: actions/github-script@v6
        id: download_ci_artifact
        with:
          script: |
            const { owner, repo } = context.repo;
            const workflow_id = 'ci-cd.yml'; // Name of your CI workflow file
            const artifact_name = 'vite-build-artifact';

            // Find the latest successful run of the CI workflow
            const { data: runs } = await github.rest.actions.listWorkflowRuns({
              owner,
              repo,
              workflow_id,
              status: 'success',
              branch: context.ref.replace('refs/heads/', ''),
            });

            if (!runs.workflow_runs.length) {
              core.setFailed(`No successful CI runs found for workflow: ${workflow_id}`);
              return;
            }

            const latestRunId = runs.workflow_runs[0].id;

            // Find the artifact ID within that run
            const { data: artifacts } = await github.rest.actions.listWorkflowRunArtifacts({
              owner,
              repo,
              run_id: latestRunId,
            });

            const artifact = artifacts.artifacts.find(a => a.name === artifact_name);

            if (!artifact) {
              core.setFailed(`Artifact "${artifact_name}" not found in run ${latestRunId}.`);
              return;
            }

            // Download and save the zip file
            const download = await github.rest.actions.downloadArtifact({
              owner,
              repo,
              artifact_id: artifact.id,
              archive_format: 'zip',
            });

            const fs = require('fs');
            fs.writeFileSync(`${artifact_name}.zip`, Buffer.from(download.data));

            // Unzip the file to the current directory
            const { execSync } = require('child_process');
            execSync(`unzip -o ${artifact_name}.zip -d .`);

      # The downloaded artifact contents are often placed in a subfolder
      # named after the artifact. We must move them to the root.
      - name: Move Build Files to Root
        run: |
          # Move 'dist' and any other folders/files from the artifact sub-folder to the workspace root
          # This command moves all contents from the artifact folder to the current directory ('.')
          mv vite-build-artifact/* .
          # Clean up the empty artifact folder and the zip file
          rm -rf vite-build-artifact
          rm -f vite-build-artifact.zip

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies and Firebase CLI
        # Functions often need node modules installed at the root for proper deployment
        run: |
          npm ci
          npm install -g firebase-tools

      - name: Construct Deployment Command
        id: deploy_command
        run: |
          DEPLOY_TARGETS=""
          case "${{ github.event.inputs.target }}" in
            "hosting")
              DEPLOY_TARGETS="hosting"
              ;;
            "functions")
              DEPLOY_TARGETS="functions"
              ;;
            "all")
              DEPLOY_TARGETS="hosting,functions"
              ;;
          esac
          echo "command=firebase deploy --only $DEPLOY_TARGETS" >> $GITHUB_OUTPUT

      - name: Run Firebase Deployment
        run: ${{ steps.deploy_command.outputs.command }} --token ${{ secrets.FIREBASE_TOKEN }}
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
